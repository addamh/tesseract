- skill = params[:skill_id]
%h2 Search Results for #{Skill.find(skill).name}
%table{:class => "table table-striped table-hover"}
  %thead
    %th Name
    %th Username
    %th Website
    %th Last Seen
    %tbody
    - @users.each do |u|
      %tr
      %td= link_to u.name, u
      %td= u.username
      %td= u.website
      %td= u.last_seen
      %tr/
  %tbody/
  %table/