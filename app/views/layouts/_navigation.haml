= image_tag "tesseractwhite2.png", :size => "45x35", :class => 'brand', :style => "padding-right: 0px; padding-bottom: 2px; padding-top: 3px;"
= link_to "Tesseract", root_path, :class => 'brand'
%ul.nav
  - if user_signed_in?
    %li
      %a
        Jobs
    %li
      = link_to "Developers"
    %li
      = link_to "Employers"
    %li
      = link_to "My Profile", user_path(current_user.id)
%ul.nav{:style => "float: right;"}
  - if user_signed_in?
    %li.dropdown
      %a.dropdown-toggle{:href => "#", :data => {:toggle => "dropdown"}}
        - if current_user.current_avatar != nil
          - avatar_url = Service.find(current_user.current_avatar).avatar_url
          %img{:src => current_user.avatar_url, :height => 25, :width => 25, :style => "border: 1px solid grey; margin-right: 5px;"}
        = current_user.name
        %b.caret
      %ul.dropdown-menu
        %li 
          = link_to "Edit Profile", edit_user_path(current_user.id)
        %li 
          = link_to "Settings", services_path
        %li
          = link_to("Logout", signout_services_path)
  - else 
    %li
      = link_to "Sign In", signin_services_path